<?xml version="1.0" encoding="utf-8"?>
<!-- ARCHITECTURE.md §12: strings live here from the first commit; no
     hardcoded UI text. §5.6: no string here may interpolate a server
     address, uuid, or key. -->
<resources>
    <string name="app_name">Subspace</string>

    <!-- SubspaceNavHost placeholder for the Editor route. Task 16 only
         builds the navigation skeleton — Editor gets its real screen from a
         later task, which replaces this content outright rather than build
         on top of it. QrScan's own placeholder (Task 16) was replaced by
         the real QrScanScreen in Task 20's fix round 1. profileId is a
         local Room row id, not a secret (§5.6 covers server
         addresses/uuids/keys, not row ids). -->
    <string name="nav_editor_placeholder_title">Editor — profile #%1$d</string>
</resources>
